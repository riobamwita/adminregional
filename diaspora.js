import{supabase}from"./supabase.js";import{requireAdmin}from"./admin-guard.js";const $=id=>document.getElementById(id),grid=$("grid"),modal=$("modal"),esc=v=>String(v??"—").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m])),date=v=>v?new Date(v).toLocaleString("en-KE",{dateStyle:"medium",timeStyle:"short"}):"—",returnDate=v=>v?new Date(v+"T00:00:00").toLocaleDateString("en-KE",{dateStyle:"medium"}):"Not specified";let data=[],current=null;const statuses=["new","contacted","processing","completed","cancelled"];async function load(){$("loading").style.display="block";grid.innerHTML="";$("empty").style.display="none";$("error").classList.remove("active");try{const{data:d,error}=await supabase.from("returning_resident_requests").select("*").order("created_at",{ascending:false});if(error)throw error;data=d||[];stats();render()}catch(e){$("error").textContent=e.message;$("error").classList.add("active")}finally{$("loading").style.display="none"}}function stats(){$("total").textContent=data.length;$("new").textContent=data.filter(x=>x.status==="new").length;$("contacted").textContent=data.filter(x=>x.status==="contacted").length;$("completed").textContent=data.filter(x=>x.status==="completed").length;$("vehicleCount").textContent=data.filter(x=>x.vehicle_interest).length}function render(){const q=$("search").value.toLowerCase().trim(),s=$("filter").value,o=$("sort").value,l=data.filter(x=>`${x.full_name||""} ${x.phone||""} ${x.email||""} ${x.country||""} ${x.kra_pin||""} ${x.vehicle_interest||""}`.toLowerCase().includes(q)&&(s==="all"||x.status===s));l.sort((a,b)=>o==="old"?new Date(a.created_at)-new Date(b.created_at):o==="return"?new Date(a.expected_return_date||"9999-12-31")-new Date(b.expected_return_date||"9999-12-31"):new Date(b.created_at)-new Date(a.created_at));if(!l.length){$("empty").style.display="block";return}grid.innerHTML=l.map(x=>`<article class="diaspora-card" data-id="${esc(x.id)}"><div class="diaspora-card-top"><span class="diaspora-status ${esc(x.status||"new")}">${esc(x.status||"new")}</span><small>${date(x.created_at)}</small></div><h3>${esc(x.full_name)}</h3><p><i class="fa-solid fa-earth-africa"></i>${esc(x.country)}</p><p><i class="fa-solid fa-phone"></i>${esc(x.phone)}</p><p><i class="fa-solid fa-calendar"></i>Return: ${returnDate(x.expected_return_date)}</p><div class="diaspora-card-meta"><div><span>Vehicle Interest</span><strong>${esc(x.vehicle_interest||"Not specified")}</strong></div><div><span>Contact</span><strong>${esc(x.contact_method||"WhatsApp")}</strong></div></div><div class="diaspora-card-bottom"><span>${x.passport_url?'<i class="fa-solid fa-file-shield"></i> Passport uploaded':'No passport'}</span><button data-action="view" type="button">View Request <i class="fa-solid fa-arrow-right"></i></button></div></article>`).join("")}function field(label,value){return`<div class="detail-field"><span>${label}</span><strong>${esc(value)}</strong></div>`}async function viewRequest(id){current=data.find(x=>x.id===id);if(!current)return;$("modalTitle").textContent=current.full_name;$("modalDate").textContent=`Submitted ${date(current.created_at)}`;$("status").value=current.status||"new";let passport="";if(current.passport_url){const{data:signed,error}=await supabase.storage.from("returning-resident-passports").createSignedUrl(current.passport_url,3600);if(!error&&signed?.signedUrl)passport=`<div class="passport-box"><div><i class="fa-solid fa-passport"></i><span>${esc(current.passport_file_name||"Passport document")}</span></div><a href="${esc(signed.signedUrl)}" target="_blank" rel="noopener"><i class="fa-solid fa-arrow-up-right-from-square"></i> Open Passport</a></div>`;else passport=`<div class="passport-box unavailable"><i class="fa-solid fa-lock"></i> Passport document unavailable</div>`}else passport=`<div class="passport-box unavailable"><i class="fa-solid fa-file-circle-xmark"></i> No passport uploaded</div>`;$("modalBody").innerHTML=`<div class="detail-section"><h3>Customer Information</h3><div class="detail-grid">${field("Full Name",current.full_name)}${field("Phone",current.phone)}${field("Email",current.email)}${field("Country",current.country)}${field("KRA PIN",current.kra_pin)}${field("Contact Method",current.contact_method)}</div></div><div class="detail-section"><h3>Return Information</h3><div class="detail-grid">${field("Expected Return",returnDate(current.expected_return_date))}${field("Duration Abroad",current.duration_abroad)}${field("Vehicle Interest",current.vehicle_interest)}${field("Status",current.status)}</div></div><div class="detail-section"><h3>Passport</h3>${passport}</div><div class="detail-section"><h3>Additional Notes</h3><div class="detail-message">${esc(current.notes||"No additional notes provided.")}</div></div>`;modal.classList.add("show");document.body.classList.add("locked")}async function save(){if(!current)return;const status=$("status").value,{error}=await supabase.from("returning_resident_requests").update({status,updated_at:new Date().toISOString()}).eq("id",current.id);if(error)return alert(error.message);const x=data.find(x=>x.id===current.id);if(x)x.status=status;current.status=status;close();stats();render()}async function remove(){if(!current||!confirm(`Delete registration from ${current.full_name}?`))return;const{error}=await supabase.from("returning_resident_requests").delete().eq("id",current.id);if(error)return alert(error.message);close();load()}function close(){modal.classList.remove("show");document.body.classList.remove("locked");current=null}grid.addEventListener("click",e=>{const card=e.target.closest(".diaspora-card");if(card)viewRequest(card.dataset.id)});$("search").oninput=render;$("filter").onchange=render;$("sort").onchange=render;$("refreshBtn").onclick=load;$("save").onclick=save;$("delete").onclick=remove;$("close").onclick=close;$("closeBg").onclick=close;$("menu").onclick=()=>{$("sidebar").classList.add("open");$("overlay").classList.add("show")};$("closeMenu").onclick=$("overlay").onclick=()=>{$("sidebar").classList.remove("open");$("overlay").classList.remove("show")};$("logoutBtn").onclick=async()=>{await supabase.auth.signOut();location.replace("auth.html")};document.addEventListener("keydown",e=>e.key==="Escape"&&modal.classList.contains("show")&&close());window.addEventListener("load",()=>setTimeout(()=>$("loader")?.classList.add("hide"),450));requireAdmin("diaspora").then(x=>x&&load());